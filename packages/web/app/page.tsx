import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "cohost-embed",
};

export default function Home() {
  return (
    <html lang="en">
      <body>
        <span>home</span>
      </body>
    </html>
  );
}
